# ===== 多游戏工作区（2026-09-16 重组：四游戏移入 games/，见 .workbuddy/reorg_plan.md）=====
# 各游戏运行时素材入仓（GitHub Pages 需加载），仅原始生成物/草稿排除
# （原 immortal-market/AssetLibrary/AI原始图/ 行已删：素材库三合一后该目录移出仓库）
games/immortal-market/_ai_raw/
games/immortal-market/_oga_drop/
games/immortal-market/_godot_drop/

# 临时 scratch / 截图 / 日志 / 缩略图集
_*.png
_*.txt
_*.html
_*.webp
_*.err
_*.out
games/immortal-market/_*.png
games/immortal-market/_*.txt
games/immortal-market/_*.html

# 本工作目录里的 WorkBuddy 元数据 / 手工备份（bak_*）—— 绝不入库
.workbuddy/
# `_` 前缀的一次性分析脚本（正式工具无下划线前缀）
games/immortal-isles/tools/_*.py
games/immortal-isles/tools/_*.js
# Python 字节码缓存
__pycache__/

# Windows 重定向残留（cmd 的 NUL 在 bash 下落成实体文件）
nul

# 用户提供的原始素材包（源材料，供派生脚本读取；非运行时资源，不入库）
人物角色/
地形包/

# 临时分析目录（切片草稿、量测输出、预览图），不属于运行时资源
_analysis/
games/immortal-isles/_analysis/
# 妖兽单帧裁切源（策展中间物；运行时只用 assets/foes_atlas.png/.json）
games/immortal-isles/assets/foes/
# 物品图标的 2× 高清预览（只是给 tools/gen_items_atlas.py 人工核对用的中间物；
# 背包格子直接用 items_atlas.png 里 64px 帧放大，不读这张）
games/immortal-isles/assets/items_atlas_big.png

# 外来地图的中间瓦片（tools/import_tmx.py 产物；运行时只用 <prefix>_atlas.webp/.json）
games/immortal-isles/assets/imported/
games/immortal-isles/assets/gr_iso/
games/immortal-isles/assets/flare/
games/immortal-isles/assets/flare_*/

# 用户提供的原始 AI 场景素材（大体积 JPEG，源材料非运行时资源，不入库）
sucai/

# 第三方 CC0 素材源缓存（Screaming Brain Studios 等；派生结果都已在 assets/ 下）
games/immortal-isles/_cc0/

# 2026-09-16 重组新增：工作区级归档与公共素材库（本地资产，不入公共仓库）
_archive/
assets_library/
